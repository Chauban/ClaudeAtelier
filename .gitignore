# 原图只留本地，不进仓库（体积太大）
Cards/*/
!Cards/cards-index.csv

# 各模型自己目录下的原图同理（deepseekv4flash/Cards/2026-08/ 之类）
*/Cards/*/
!*/Cards/cards-index.csv

# 给 AI 看的文档只留本地，不公开到网站和仓库。
# 用通配符而不是逐个列名：生卡任务收尾会 git add -A，新写的 HANDOFF
# 文档只要没在这里，就会被扫进那条「NO.xxxx · 风格 · 语言」的提交里。
HANDOFF-*.md
CLAUDE.md

_recovered/
thumbs/
*.tmp
Thumbs.db
desktop.ini
航空信封邮票-20260729.png
sync.log
sync-last-run.log

# 本地触发器的日志。它每 4 小时写一行，不该进仓库，
# 更不该在 Claude 那条线 git add -A 的时候被扫进去。
trigger.log

# Python 缓存
__pycache__/
*.pyc

# DeepSeek 原图取回任务的状态与日志
.ds-fetch-state.json
fetch-ds.log
